language: r
cache: packages
sudo: false

r_packages:
        - rmarkdown
        - revealjs
          
install: 
        - echo "No install needed"

script:
        - make

deploy:
        provider: pages
        skip_cleanup: true
        github_token: $GITHUB_TOKEN
        on:
                branch: master


